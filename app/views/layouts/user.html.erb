<!DOCTYPE html>
<html>
  <head>
    <title>WebfieldRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="flex">
        <nav class="flex ">
            <div class="w-64 p-4 h-screen bg-gray-200 sticky top-0 flex flex-col items-betweem">
            <div>
                <div class="flex flex-col items-start justify-start">
                  <%= link_to root_path do%>
                    
                    <%= image_tag"thewebfield_logo_bk.png", :alt => "rss feed", class: " w-[150px]" %>
                  <% end%>
                </div>
                <ul class="flex flex-col mt-8">
                  <li>
                    <%= link_to user_path, class: "block px-4 py-2 hover:rounded hover:bg-gray-700 hover:text-gray-200" do %>
                      <span class="hidden md:inline-block">Dashboard</span>
                    <% end %>
                  </li>
                  <li>
                    <%= link_to user_projects_path, class: "block px-4 py-2 hover:rounded hover:bg-gray-700 hover:text-gray-200" do %>
                      <span class="hidden md:inline-block">Projects</span>
                    <% end %>
                  </li>
                  <li>
                    <%= link_to tasks_path, class: "block px-4 py-2 hover:rounded hover:bg-gray-700 hover:text-gray-200" do %>
                      <span class="hidden md:inline-block">Tasks</span>
                    <% end %>
                  </li>
                  <li>
                    <%= link_to user_path, class: "block px-4 py-2 hover:rounded hover:bg-gray-700 hover:text-gray-200" do %>
                      <span class="hidden md:inline-block">Contracts</span>
                    <% end %>
                  </li>
                </ul>
              </div>
              <%= link_to destroy_user_session_path, data: { "turbo-method": :delete, "turbo-confirm": "Are you sure you want to sign out?" }, class: "block px-4 py-2 hover:rounded hover:bg-gray-700 hover:text-gray-200" do %>
                <span class="hidden md:inline-block">Sign out</span>
             <% end %>
            </div>
        </nav>
        <div class="w-full p-8">
        <%= yield %>
        </div>
    </main>
  </body>
</html>

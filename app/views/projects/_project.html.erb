<div id="<%= dom_id project %>">
<%= render_button("< Back to projects", text: nil,
                                        variant: :link,
                                        as: :link,
                                        href: projects_path) %>
<h1 class="font-bold text-4xl mb-3 mt-5"><%= project.name %></h1>
 

  <p class="my-5">
    <strong class="block font-medium mb-1">Description:</strong>
    <%= project.description %>
  </p>
</div>
